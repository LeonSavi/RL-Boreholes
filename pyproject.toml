[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[project]
name = "rl-boreholes"
version = "0.1.0"
description = "Mining exploration thesis project"
requires-python = ">=3.10"

[tool.setuptools.packages.find]
where = ["."]
include = ["decision_simulator*"]